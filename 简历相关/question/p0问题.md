# 翟思丰 P0 面试题答案稿

来源：`interview_question_bank_zhaisifeng.md` 中所有 `P0` 问题。

使用原则：

- 先背 `推荐回答`，再根据追问展开。
- 数据只按简历口径说，不临场新增不可证明的数字。
- 遇到边界问题主动说明：哪些是个人负责，哪些是团队结果。

## Q1：请做一个 1 分钟自我介绍。

### 速记链路

```text
个人定位
  -> React/Next.js Web + React Native 跨端
  -> 睿联：Reolink Web 商城 + RN 智能安防 App
  -> 淘腾：GogoalShop 性能优化 + Shopify 自研应用
  -> 核心优势：商城业务、性能优化、跨端、工程化
```

### 推荐回答

面试官您好，我叫翟思丰，有 3 年前端开发经验，核心方向是 React/Next.js Web 前端与 React Native 跨端开发，一直深耕跨境出海赛道。

目前在睿联软件，我同时负责 Reolink 跨境商城 Web 端与智能安防 RN 移动端两条业务线。Web 侧主导核心页面研发、SEO 落地与首屏性能优化；移动端负责设备接入、实时预览等核心链路的功能迭代、多端适配与测试体系建设。

之前在淘腾网络，我牵头完成了跨境商城首页性能专项优化，将关键 JS 从 3MB 压缩至 600KB，首页 LCP 从 4s 降至 2s 以内；同时自研两款 Shopify 应用并完成上架，替代第三方付费工具降低业务成本。

我的核心优势是 React 全栈生态、性能优化、跨端落地与工程化能力，也希望能加入贵司，在前端复杂业务与工程质量方向持续深耕。

### 追问回答要点

- 核心方向：`React/Next.js Web 前端 + React Native 跨端`。
- 代表项目：Web 岗讲 `Reolink Web + GogoalShop`；跨端岗讲 `Reolink 移动端`。
- 个人主导：页面模块、核心链路、测试补充、工具建设可讲个人动作。
- 团队结果：自然流量增长、整体交付质量这类结果要讲团队协作；个人贡献落到页面、链路、测试和流程动作。

### 追问逐题回答

#### 你现在最核心的技术方向是什么，Web 前端还是跨端？

我的核心方向还是前端，主线是 React/Next.js Web 前端，同时具备 React Native 跨端项目经验。Web 侧我更熟悉商城页面、SEO、性能优化和工程化；跨端侧主要集中在 React Native 核心链路、多端适配、状态管理和测试补充。所以如果做岗位定位，我会优先定位为 `Web 前端 + React Native 跨端能力`，而不是纯 Android 原生或纯移动端。

#### 你最近一段工作中最有代表性的项目是什么？

最近最有代表性的是 Reolink 移动端项目和 Reolink Web 跨境商城。Web 项目能体现我在 React/Next.js、SEO、LCP、组件化和活动页交付上的能力；移动端项目能体现我在 React Native、核心业务链路、状态拆分、多端适配和测试体系上的能力。

#### 你说自己适合 3-5 年前端岗位，核心依据是什么？

核心依据是我不只是做页面交付，也做过性能优化、工程化治理、测试补充和业务工具建设。比如 Web 侧有商城页面、SEO、LCP 优化；跨端侧有 React Native 核心链路、状态拆分、多机型适配；工程侧有 pnpm 迁移、ESLint/Prettier/Husky 规范、Shopify Docker 部署和脚本提效。

#### 你简历里哪些成果是你个人主导，哪些是团队共同结果？

我个人主导或主要负责的部分包括具体页面模块开发、RN 侧核心链路功能迭代、组件抽象、部分性能优化动作、测试用例补充、Shopify 应用开发和部署脚本。团队共同结果包括自然流量增长、整体交付质量、业务转化这些更大的业务或质量指标。

## Q2：你为什么从 Web 前端扩展到 React Native 跨端？

### 速记链路

```text
React/TypeScript 基础
  -> RN 是 React 能力向移动端延伸
  -> 业务需要设备接入、预览、消息、回放
  -> 迁移能力：组件化、状态管理、接口联调、测试
  -> 边界：不说精通 Android/iOS 原生底层
```

### 推荐回答

我的基础能力一直是 React 和 TypeScript，React Native 对我来说不是完全换方向，而是把 React 的组件化、状态管理和工程化经验迁移到移动端场景。

在 Reolink 项目里，业务本身既有 Web 商城，也有移动端设备管理需求。移动端涉及设备接入、实时预览、消息提醒、录像回放等链路，和 Web 页面相比更关注多机型适配、性能、状态流转和稳定性。我参与 React Native 项目后，主要把 Web 侧积累的组件拆分、状态拆分、接口联调、测试补充能力迁移过去。

但我会明确边界：Android/iOS 原生不是我的主方向，我更匹配的是 React Native、Web/H5、跨端融合、前端工程化和性能优化相关岗位。如果岗位强要求 Java/Kotlin Framework，我不会说自己完全匹配。

### 追问回答要点

- 迁移动机：React 技术栈自然延伸，不是临时转型。
- RN 职责：核心链路、状态拆分、多端适配、测试补充。
- 原生边界：能和 Native Modules 协作，不说精通 Android Framework。

### 追问逐题回答

#### 你在 React Native 项目里具体负责到什么程度？

我主要负责 React Native 侧核心功能迭代和工程质量建设，重点围绕设备接入、实时预览、消息提醒、录像回放这 4 条核心链路做业务开发、状态拆分、多机型适配、长列表和页面渲染优化，以及 Jest、React Native Testing Library、Detox 测试用例补充。

**设备接入链路**

```text
进入接入入口：用户从添加设备入口进入扫码或手动输入设备信息流程。
校验基础权限：RN 侧检查相机、网络、定位或局域网相关权限是否满足接入条件。
采集设备信息：通过扫码、输入 SN 或局域网发现拿到设备标识和接入参数。
发起接入请求：调用接口或原生能力触发设备绑定、连接或配网流程。
进入连接状态：页面切到 connecting，展示进度、loading 和可取消入口。
处理成功结果：接入成功后更新设备状态，并跳转到设备详情或预览入口。
处理失败分支：接入失败时根据错误类型展示权限、网络、设备离线、超时等提示。
提供重试兜底：失败后保留用户已输入信息，提供重试、重新扫码或返回修改入口。
```

设备接入流程图：

```text
添加设备入口
  -> 扫码 / 手动输入 / 局域网发现
  -> 权限检查（相机 / 网络 / 定位 / 局域网）
  -> 采集设备标识和接入参数
  -> 调用 deviceService.bindDevice / 原生接入能力
  -> 页面状态：connecting
  -> 成功：更新设备状态 -> 进入设备详情 / 实时预览入口
  -> 失败：映射错误类型 -> 展示权限/网络/离线/超时提示 -> 保留输入 -> 重试
```

**实时预览链路**

```text
进入预览页面：用户从设备列表或设备详情点击进入实时预览页。
初始化预览参数：RN 侧准备 deviceId、码流类型、清晰度、静音状态等参数。
调用预览服务：页面调用 previewService.startPreview，而不是直接散落调用 NativeModules。
触发原生桥接层：previewService 将参数传给原生预览桥接层，由其对接底层播放能力或设备 SDK。
订阅播放事件：RN 侧通过 service 或 usePreview 订阅 loading、started、reconnect、error、stopped 等事件。
映射页面状态：将原生事件统一映射成 loading、playing、reconnecting、error、idle 等 UI 状态。
展示业务兜底：根据错误码展示权限、网络、设备离线、播放失败等可理解提示。
退出释放资源：页面卸载或切换设备时 stopPreview，并移除事件监听，避免后台播放和重复订阅。
```

实时预览我会选择这个更适合智能安防场景的方案：底层由厂商/自研设备 SDK 承接设备鉴权、P2P/Relay、码流、弱网重连和播放能力；Android/iOS 原生侧封装成统一的 `PreviewModule` 桥接层；RN 侧只对接桥接层，并通过 `previewService` / `usePreview` 管理页面状态和异常兜底。

实时预览流程图：

```text
设备列表 / 设备详情
  -> 进入 PreviewScreen
  -> 初始化 deviceId / streamType / quality / muted
  -> usePreview 调用 previewService.startPreview
  -> previewService 调用 NativeModules.PreviewModule
  -> PreviewModule 原生预览桥接层统一 Android/iOS 接口
  -> 原生桥接层调用厂商/自研设备 SDK
  -> 设备 SDK 处理鉴权、P2P/Relay、码流拉取、硬解播放、弱网重连
  -> 原生桥接层统一错误码和播放事件
  -> event emitter 回传 loading / started / reconnect / error / stopped
  -> usePreview 映射为 loading / playing / reconnecting / error / idle
  -> 页面按状态展示加载态 / 播放态 / 错误兜底 / 重连提示
  -> 页面退出：stopPreview -> remove listeners -> 释放资源
```

这个链路里我会重点讲的难点是：**弱网重连、播放状态一致性和资源释放**。实时预览不是普通接口请求，底层可能经历设备鉴权、P2P 连接、Relay 降级、拉流、首帧渲染、网络抖动和重连。如果 RN 侧只用一个 `loading` 或 `error` 状态，会把连接中、重连中、设备离线、播放失败混在一起，用户体验和问题定位都会比较差。

我的处理方式是让原生桥接层把底层 SDK 的关键状态转成统一事件，比如 `onPlayerLoading`、`onPlayerStarted`、`onNetworkReconnect`、`onPlayerError`、`onPlayerStopped`。RN 侧在 `usePreview` 里订阅这些事件，再映射成 `loading`、`playing`、`reconnecting`、`error`、`idle` 等页面状态。退出页面、切换设备或 App 进入后台时，主动调用 `stopPreview` 并移除监听，避免后台播放、重复订阅和状态错乱。
**消息提醒链路**

```text
进入消息入口：用户从首页、设备页或通知入口进入告警消息列表。
拉取消息列表：RN 侧按设备、时间、分页条件请求告警消息数据。
拆分消息状态：将消息列表、未读数、已读状态、筛选条件和页面 loading 分开管理。
展示消息内容：列表展示告警类型、时间、设备名称、缩略图和未读标识。
处理点击跳转：用户点击消息后跳转到设备详情、预览页或对应录像回放入口。
同步已读状态：点击或批量操作后更新本地状态，并按接口结果同步服务端已读状态。
处理空错分支：无消息展示空状态，接口失败展示重试入口，避免列表白屏。
补充回归验证：用组件测试覆盖列表展示和已读状态，用端到端测试覆盖点击跳转主路径。
```

消息提醒流程图：

```text
首页 / 设备页 / 通知入口
  -> 进入告警消息列表
  -> 按 deviceId / 时间 / 分页条件请求消息
  -> 拆分 messages / unreadCount / filters / pageStatus
  -> 成功：展示消息列表、未读标识、缩略图、告警类型
  -> 空数据：展示空状态
  -> 失败：展示错误提示和重试入口
  -> 点击消息：本地更新已读 -> 调用 markAsRead -> 跳转设备详情 / 预览 / 回放
  -> 回归验证：列表展示、已读状态、点击跳转
```

**录像回放链路**

```text
进入回放入口：用户从设备详情、消息提醒或预览页进入录像回放页面。
选择回放条件：RN 侧管理日期、时间段、录像类型、设备 ID 等筛选条件。
请求录像列表：按筛选条件分页拉取录像片段列表或时间轴数据。
展示回放列表：用 FlatList 或时间轴组件展示录像片段、缩略图、时长和加载状态。
触发回放播放：用户点击录像片段后调用回放服务，并进入 loading 或 playing 状态。
处理无数据场景：没有录像时展示无录像兜底，而不是空白页面。
处理失败分支：加载失败或播放失败时展示错误原因和重试入口。
优化列表性能：通过虚拟化参数、item memo、缩略图缓存和分页加载减少卡顿。
```

录像回放流程图：

```text
设备详情 / 消息提醒 / 实时预览
  -> 进入回放页面
  -> 设置 deviceId / 日期 / 时间段 / 录像类型
  -> 请求 playbackService.getRecords
  -> 成功：渲染录像列表 / 时间轴 / 缩略图
  -> 空数据：展示无录像兜底
  -> 失败：展示错误提示和重试入口
  -> 点击录像片段：进入 PlaybackPlayer -> 调用回放播放能力
  -> 长列表优化：FlatList 虚拟化参数 -> item memo -> 缩略图缓存 -> 分页加载
```


状态拆分上，我主要拆成三类：设备状态、消息状态、页面展示状态。设备状态包括在线/离线、连接中、预览中、回放中、连接失败；消息状态包括消息列表、未读数、已读状态、跳转入口；页面展示状态包括 loading、弹窗、tab、筛选条件、分页、空状态和错误提示。简单 UI 状态用 `useState`，复杂流程比如设备连接和回放状态用 `useReducer`，跨页面复用逻辑抽成 `useDeviceStatus`、`useMessageList` 这类自定义 Hook。

多机型适配上，我主要从屏幕尺寸、安全区、系统版本、字体缩放和平台差异处理。比如用 `react-native-safe-area-context` 处理刘海屏和底部安全区，用 `Platform.select` 处理少量 Android/iOS 差异；布局上避免固定写死宽高，重点验证设备管理、列表浏览、预览操作、底部操作区这些高频页面。

多机型适配流程图：

```text
拿到设计稿和核心页面
  -> 梳理高频页面：设备管理 / 列表浏览 / 实时预览 / 底部操作区
  -> 定义适配维度：屏幕尺寸 / 安全区 / 系统版本 / 字体缩放 / 平台差异
  -> 布局实现：弹性布局 + 百分比 / flex + 避免固定写死宽高
  -> 安全区处理：SafeAreaProvider + useSafeAreaInsets
  -> 平台差异处理：Platform.select 隔离 Android / iOS 差异
  -> 字体和间距兜底：限制异常字体缩放导致的布局溢出
  -> 真机验证：覆盖小屏 / 大屏 / 刘海屏 / Android / iOS
  -> 问题回收：记录兼容问题 -> 收敛成页面适配规范
```

长列表优化上，我会拿录像回放列表举例：一个摄像头如果开启移动侦测或事件录像，单日可能产生几十到几百条录像片段；高频场景下可能到 300-500 条。如果用户切换日期或查看多个设备，列表数据会继续累积。这种场景不能用 `ScrollView` 直接全量渲染，而是用 `FlatList` / `SectionList` 分页加载，比如每页 20 条。优化点包括稳定 `keyExtractor`，合理设置 `initialNumToRender`、`windowSize`、`maxToRenderPerBatch`、`removeClippedSubviews`；列表项用 `React.memo`，事件函数用 `useCallback`，复杂计算用 `useMemo`，图片使用缩略图和缓存，避免列表项里做重计算。定位时会结合 RN Performance Monitor、Flipper、React DevTools Profiler，必要时再看 Android Studio Profiler 或 Xcode Instruments。

长列表优化流程图：

```text
识别列表场景：录像回放列表单日几十到几百条，高频设备约 300-500 条
  -> 避免 ScrollView 全量渲染，改用 FlatList / SectionList
  -> 数据侧处理：每页 20 条 / 游标加载 / 下拉刷新 / 空状态
  -> 虚拟化参数：initialNumToRender / windowSize / maxToRenderPerBatch
  -> 渲染稳定性：稳定 keyExtractor / renderItem / getItemLayout
  -> Item 优化：React.memo / useCallback / useMemo / 避免重计算
  -> 图片优化：缩略图 / 缓存 / 懒加载
  -> 性能定位：RN Performance Monitor / Flipper / React DevTools Profiler
  -> Native 侧排查：必要时用 Android Studio Profiler / Xcode Instruments
```

FlatList 适合普通长列表，比如设备列表、消息列表、录像片段列表；SectionList 适合天然有分组结构的列表，比如按日期分组的录像回放、按设备分组的告警消息、按月份分组的历史记录。

它们底层都基于 VirtualizedList，都支持虚拟化渲染、keyExtractor、onEndReached、initialNumToRender、windowSize 等优化思路。区别是 SectionList 多了 section header、section footer、renderSectionHeader 这类分组能力。

虚拟化渲染不是减少数据量，而是减少同一时间真实参与渲染的视图数量。比如录像回放有 500 条数据，如果全量渲染，会一次性创建 500 个 item、图片、文本和事件绑定；FlatList 会只渲染当前屏幕附近的几十条，其余通过占位高度保持滚动区域，等用户滚动到对应位置时再挂载新的 item，并回收远离屏幕的 item。

#### Web 前端经验对 React Native 项目有什么帮助？

帮助主要在 React 组件化、TypeScript 类型约束、状态拆分、接口联调、性能分析和测试意识上。React Native 不是浏览器环境，但组件设计、props 边界、自定义 Hook、状态粒度、重复渲染治理这些思路是相通的。Web 项目里积累的工程化和问题拆解方法，也能迁移到移动端项目。

## Q3：你简历里的 AI skills / AI Agent 辅助研发具体是什么？

### 速记链路

```text
不是模型训练
  -> 是 AI 辅助研发流程
  -> 5 类场景：需求、联调、排查、测试、文档
  -> 10+ 结构化模板
  -> 人工 review + lint/test/build 验证
```

### 推荐回答

我这里说的 AI skills / AI Agent，不是模型训练，也不是简单让 AI 帮忙写代码，而是把一些重复性、上下文依赖强的研发动作沉淀成结构化流程。

在 Reolink 移动端项目里，设备接入、消息提醒、录像回放这些链路上下文比较长，新人或者跨模块协作时容易出现需求理解不一致、异常排查依赖经验、文档沉淀分散的问题。所以我把流程拆成 5 个环节：需求拆解、接口联调、异常排查、测试补充、文档沉淀。

具体做法是沉淀 10+ 结构化任务模板，比如需求拆解模板会要求说明业务背景、入口页面、接口依赖、异常分支、验收标准；异常排查模板会要求先列日志、复现路径、影响范围，再让 AI 辅助给排查方向。AI 输出后不会直接采纳，会经过代码 review、测试用例、lint/build 或人工验证。

所以它的价值不是替代工程判断，而是把上下文组织、重复分析和文档沉淀流程化。

### 追问逐题回答

#### 不是概念包装的话，具体落地在哪些环节？

具体落地在 5 个环节：需求拆解、接口联调、异常排查、测试补充、文档沉淀。比如需求拆解会要求明确业务背景、入口页面、接口依赖、异常分支和验收标准；异常排查会先输入复现路径、日志和影响范围，再让 AI 辅助给排查方向；测试补充会根据核心链路生成用例清单，再由人工筛选和实现。

#### 10+ 结构化任务模板分别覆盖哪些场景？

主要覆盖需求拆解、接口联调记录、异常排查、代码改动说明、测试用例补充、上线检查、问题复盘、文档沉淀等场景。它们不是复杂系统，核心价值是把输入信息标准化，减少每次从零组织上下文。

#### AI 生成结果怎么校验？

我不会直接采纳 AI 输出。代码类结果要经过 diff review、lint、test、build 和人工验证；问题排查类结果要回到日志、复现路径和实际代码验证；文档类结果要和真实业务流程、接口字段、异常分支对齐。AI 只负责辅助组织和初稿，最终判断仍然由人和验证结果决定。

#### 这套流程对新人上手复杂业务有什么实际帮助？

帮助主要是降低上下文获取成本。新人面对设备接入、消息提醒、录像回放这类长链路时，容易不知道该问什么、看哪些接口、验证哪些异常分支。结构化模板把业务背景、入口、接口、异常、验收标准固定下来，新人可以按模板补信息和排查，减少依赖口口相传。

#### 你是否做过 AI 模型训练或移动端模型部署？

没有。我做的是 AI 辅助研发流程，不是 TensorFlow、PyTorch 模型训练，也不是移动端模型部署。我的 AI 经验更偏工程协作和研发提效，比如需求拆解、代码草拟、异常排查、测试补充和文档沉淀。

## Q4：介绍一下 Reolink Web 跨境商城项目。

### 速记链路

```text
项目背景：智能安防官网商城
  -> 负责页面：商品页、活动页、解决方案页
  -> 技术动作：组件化、响应式、SEO、LCP
  -> 流程沉淀：活动页检查清单、联调手册
  -> 结果：10+ 页面、8+ 组件、20+ SEO 页面
```

### 推荐回答

Reolink Web 跨境商城是面向海外用户的智能安防官网商城，核心目标是承接商品展示、新品发布、营销活动和内容运营，同时兼顾 SEO、性能和多端体验。

我主要负责商品详情页、活动落地页、解决方案页等 10+ 核心页面开发。技术栈主要是 React、Next.js、TypeScript、TailwindCSS、Axios。项目中我做了几类工作：

第一是核心页面研发，保障商品信息、活动内容、购买入口在桌面端、平板端、移动端展示一致。

第二是组件化沉淀，把商品卡片、卖点模块、媒体展示、规格参数、FAQ 抽成 8+ 可复用业务组件，减少后续活动页重复开发。

第三是性能和 SEO，通过图片懒加载、路由级代码分割、语义化标签、Meta 信息配置等方式优化页面加载和搜索收录，完成 20+ 商品/活动页面基础 SEO 配置，重点活动页 LCP 控制在 2.5s 内。

第四是协作流程沉淀，和设计、运营、后端对齐需求、接口联调和上线验收，沉淀活动页检查清单和常见联调问题处理方式。

### 追问回答要点

- 项目定位：官网商城，不是普通后台系统。
- 和 GogoalShop 区别：Reolink 更偏品牌官网、商品内容、SEO 与活动转化；GogoalShop 更偏独立电商商城性能治理和工具建设。
- 10+ 页面：商品详情页、活动落地页、解决方案页、内容运营页等。

### 追问逐题回答

#### 这个项目的业务目标是什么？

业务目标是服务海外智能安防用户，通过官网商城承接商品展示、新品发布、营销活动和内容运营，提升用户对产品的理解、搜索收录和转化链路体验。技术上要兼顾多端展示一致性、SEO、LCP 性能和活动页交付效率。

#### 你负责哪些页面和模块？

我主要负责商品详情页、活动落地页、解决方案页等核心页面，以及商品卡片、卖点模块、媒体展示、规格参数、FAQ 等复用组件。同时参与 SEO 配置、响应式适配、性能优化和上线检查清单沉淀。

#### 商品详情页、活动落地页、解决方案页分别有什么差异？

商品详情页重点是商品信息完整、规格参数清晰、购买入口明确；活动落地页重点是营销视觉、活动规则、跳转路径和上线时效；解决方案页更重内容表达和场景化说明，比如家庭安防、商用安防。三类页面都需要 SEO 和响应式，但内容结构和转化目标不同。

#### 10+ 核心页面是哪些类型？

主要包括商品详情页、活动落地页、解决方案页、内容运营页和部分营销承接页。面试时我不会逐个报具体 URL，而是按页面类型说明，因为重点是这些页面背后的开发模式、SEO、组件复用和交付流程。

#### 这个项目和 GogoalShop 都是跨境商城，差异在哪里？

Reolink Web 更偏品牌官网商城，重点是智能安防产品展示、内容运营、SEO 和活动转化；GogoalShop 更偏独立电商商城，重点是首页性能专项、资源体积治理、工程化迁移和多语言工具建设。前者更强调页面内容和自然流量，后者更强调性能治理和工程效率。

## Q5：LCP 是怎么测的？

### 速记链路

```text
先定位 LCP 元素
  -> Lighthouse 看 Web Vitals
  -> Performance 看主线程和瀑布流
  -> 常见元素：Banner / 商品主图 / 活动主视觉
  -> 优化：图片、首屏 JS、字体、接口、资源优先级
  -> 边界：重点页面验证，不说全站绝对值
```

### 推荐回答

LCP 我一般分两类看：实验室数据和实际表现。

实验室侧主要用 Lighthouse 和 Chrome DevTools Performance。Lighthouse 看整体 Web Vitals，Performance 看主线程、Network 瀑布流和 LCP element，先确定最大内容元素到底是首屏 Banner、商品主图、活动主视觉图，还是文本块。

实际表现侧，如果项目已有 Web Vitals 上报，就结合线上采样数据看趋势；如果没有固定上报系统，我不会说成接了内部监控，而是以 Lighthouse、Performance 和上线前后的对比数据作为主要口径。

Reolink Web 重点活动页里，LCP 主要受首屏大图、字体加载、首屏 JS 执行和接口返回影响。优化时我主要做了图片压缩与合理尺寸裁剪、非首屏图片懒加载、首屏图避免误 lazy、路由级代码分割、减少首屏无关 JS、字体加载优化。最终重点活动页在 Lighthouse / Web Vitals 验证中 LCP 控制在 2.5s 内。

### 追问回答要点

- 工具：`Lighthouse`、`Chrome DevTools Performance`、可选 Web Vitals 上报。
- 图片型 LCP：尺寸、格式、CDN、priority/preload、避免首屏 lazy。
- 文本型 LCP：字体加载、CSS 阻塞、SSR/SSG 输出。
- 数据边界：不默认说接入内部监控平台。

### 追问逐题回答

#### 你用的是 Lighthouse、Chrome Performance 还是线上 Web Vitals 上报？

主要用 Lighthouse 和 Chrome DevTools Performance 做实验室分析。Lighthouse 看整体 Web Vitals，Performance 看主线程、网络瀑布流和 LCP element。如果项目已有 Web Vitals 上报，我会结合线上采样趋势；如果没有固定监控系统，我不会说接了内部平台，只按本地测量和上线前后对比来讲。

#### LCP element 通常是什么？

商城页面里常见 LCP element 是首屏 Banner、商品主图、活动主视觉图，或者首屏最大标题块。具体不能凭经验猜，要在 Lighthouse 或 Performance 里看 LCP marker，先确认它是图片还是文本，再决定优化方向。

#### 如果 LCP 元素是首屏 Banner，你会怎么优化？

我会先检查图片尺寸、格式、压缩质量和加载优先级。首屏 Banner 不应该被 lazy loading 误伤，可以考虑 priority 或 preload 思路；同时使用合适尺寸和响应式图片，避免移动端加载桌面端大图。还要检查首屏 JS 是否阻塞渲染，以及 CSS 和字体是否影响首屏展示。

#### 如果 LCP 元素是文本块，你会怎么优化？

文本型 LCP 重点看字体加载、CSS 阻塞和服务端输出时机。比如使用 next/font 或字体预加载策略减少字体阻塞，控制关键 CSS，确保首屏文本能在 SSR/SSG 阶段尽快输出，而不是等客户端接口回来才渲染。

#### 2.5s 内这个数据是实验室数据还是线上采样趋势？

更稳的说法是：重点活动页通过 Lighthouse 和 Performance 做上线前后对比，LCP 控制在 2.5s 内；如果项目有 Web Vitals 上报，再结合线上采样趋势观察。不要把它说成所有用户、所有网络环境下的绝对值。

#### 没有固定监控系统时，你怎么避免数据说不清？

我会明确说明数据来源：本地 Lighthouse、Chrome Performance、Network 瀑布流、构建体积和上线前后对比。没有固定监控系统就不说“线上监控证明”，而是讲实验室测量方法、测试页面、测试环境和优化前后对比。

## Q6：SEO 具体做了哪些？

### 速记链路

```text
目标：让页面可理解、可索引
  -> Meta：title / description / canonical / OG
  -> 结构：h1/h2/section/article
  -> 内容：商品卖点、规格、FAQ 静态输出
  -> 图片：alt、尺寸、懒加载
  -> 验收：源码可见、移动端可访问
```

### 推荐回答

SEO 我主要做基础 SEO 和页面结构优化，目标是让商品页、活动页、解决方案页更容易被搜索引擎理解和收录。

第一是 Meta 信息维护，比如 title、description、canonical、open graph，确保不同商品和活动页面有明确主题。

第二是语义化结构，页面保留清晰的 h1、h2、section、article 层级，避免核心内容全部由 div 堆叠。

第三是内容可索引，商品标题、卖点、规格参数、FAQ、活动规则尽量在 SSR/SSG 阶段输出，不把核心内容完全依赖客户端接口渲染。

第四是图片和媒体优化，包括图片 alt、尺寸控制、非首屏懒加载，避免图片资源拖慢页面。

当时覆盖了 20+ 商品/活动页面的基础 SEO 配置，和性能优化一起支撑自然流量获取。

### 追问回答要点

- SEO 不是“堆关键词”，而是 `元信息 + 结构 + 可索引内容 + 性能`。
- SSR/SSG 对 SEO 友好，纯 CSR 对搜索引擎理解有不确定性。
- 验收方式：看 HTML 源码、Meta、移动端访问、核心内容是否可见。

### 追问逐题回答

#### title、description、canonical、OG 分别解决什么问题？

title 是页面标题，影响搜索结果和页面主题判断；description 是摘要，帮助搜索引擎和用户理解页面内容；canonical 用来声明规范 URL，避免重复内容分散权重；OG 信息主要服务社交分享展示，比如分享标题、描述和图片。

#### h1/h2/section/article 这些语义化标签怎么使用？

页面通常只保留一个主 h1，表示页面核心主题，比如商品名或活动主题。h2/h3 用于商品卖点、规格参数、FAQ 等层级。section/article 用来表达内容区块，不把所有内容都堆在 div 里。这样搜索引擎和辅助工具更容易理解页面结构。

#### 商品卖点、规格参数、FAQ 怎么保证可索引？

核心内容尽量在 SSR/SSG 阶段输出到 HTML，而不是完全依赖客户端接口渲染。商品卖点、规格参数、FAQ 这些内容要有稳定的文本结构和语义标签。上线前可以检查页面源码或禁用 JS 的可见内容，确认搜索引擎能拿到主要信息。

#### SSR/SSG/CSR 对 SEO 的影响是什么？

SSR 和 SSG 更利于 SEO，因为核心 HTML 内容在首屏请求时就能返回。CSR 完全依赖浏览器执行 JS 后再渲染，搜索引擎虽然能处理部分 JS，但稳定性和时效性不如 SSR/SSG。商品页、活动页、解决方案页这类需要自然流量的页面，优先考虑 SSR/SSG。

#### 图片 alt 怎么写才不是形式主义？

alt 应该描述图片和业务内容的关系，而不是堆关键词。比如商品主图可以写产品型号和核心场景，卖点图可以描述功能点。装饰性图片可以留空或弱化，避免所有图片都写重复关键词。

#### 20+ 商品/活动页面 SEO 配置怎么验收？

验收时看每个页面的 title、description、canonical、h1、核心内容、图片 alt 是否完整；检查 HTML 源码里是否有核心内容；移动端访问是否正常；必要时用 Lighthouse SEO 检查基础问题。这个验收偏基础 SEO，不会说成完整搜索排名提升。

## Q11：介绍一下 Reolink 移动端项目。

### 速记链路

```text
项目背景：智能安防 RN App
  -> 核心链路：设备接入、实时预览、消息、回放
  -> 技术动作：状态拆分、多机型适配、性能优化、测试
  -> 原生协作：对接 Native Modules，不冒领底层播放器
  -> 结果：提升核心链路迭代和回归验证效率
```

### 推荐回答

Reolink 移动端项目是一个智能安防设备管理类 React Native 项目，核心场景包括设备接入、实时预览、消息提醒、录像回放。用户通过移动端统一管理家庭或商用安防设备。

我主要负责移动端核心模块研发和迭代，技术栈包括 React Native、TypeScript、React Hooks、Axios、React Navigation、Native Modules。我的工作主要分为几块：

第一是核心链路开发，负责设备接入、实时预览、消息提醒、录像回放 4 条核心链路的 RN 侧功能迭代。

第二是状态与模块治理，基于 React Hooks 拆分设备状态、消息状态、页面展示状态 3 类核心状态，减少页面耦合，提升复杂交互下的可维护性。

第三是多端兼容和性能优化，针对 20+ 主流机型、不同分辨率和系统版本做适配，围绕冷启动、页面渲染、长列表卡顿做懒加载、状态细粒度拆分和组件复用。

第四是测试体系建设，围绕设备接入、消息提醒、录像回放补充 20+ Jest、React Native Testing Library 和 Detox 用例，覆盖状态流转、页面交互和异常分支。

### 追问回答要点

- 职责边界：负责 RN 侧页面、调用封装、状态接入和异常兜底，不把原生底层能力说成个人负责。
- RN 收益：复用 React 生态、双端迭代效率、统一部分业务逻辑。
- 原生边界：Native Modules 协作，不说自己精通原生架构。

### 追问逐题回答

#### 这个 App 的核心业务链路是什么？

核心链路是设备接入、实时预览、消息提醒和录像回放。设备接入解决用户把安防设备接入账号的问题；实时预览解决查看摄像头画面的问题；消息提醒解决告警通知和未读状态；录像回放解决历史视频查看。

#### 设备接入、实时预览、消息提醒、录像回放分别有什么技术难点？

设备接入难点是权限、网络状态、连接状态和失败重试；实时预览难点是播放状态、加载体验、异常兜底和原生能力协作；消息提醒难点是未读状态、列表更新和点击跳转；录像回放难点是时间筛选、列表加载、无数据兜底和播放入口状态。

#### 你说负责 RN 侧功能开发，和原生侧的边界怎么划分？

我的边界是 RN 侧页面、状态流转、调用层封装、错误码处理和 UI 兜底。涉及系统权限、推送、音视频、设备 SDK 这类能力时，我会通过 Native Modules 或项目里的原生桥接层协作联调，但不会把底层原生 SDK 或播放器能力说成我个人负责。

#### 你在项目里是主导还是参与？

我会按模块边界讲：RN 侧具体页面、状态拆分、异常分支、测试用例补充可以说负责或主要参与；涉及整体 App 架构、原生底层能力、产品规模和全局质量结果，要说团队协作完成。

#### React Native 在这个项目里的主要收益是什么？

主要收益是复用 React/TypeScript 生态，提升双端业务迭代效率，同时让一部分业务逻辑、组件和状态管理方式在 iOS/Android 间保持一致。但涉及系统权限、推送、音视频、设备能力时，仍然需要 Native Modules 或原生 SDK 协作。

## Q12：React Native 状态怎么拆？

### 速记链路

```text
不放一个大状态
  -> 设备状态：在线、连接、预览、回放
  -> 消息状态：列表、未读、跳转
  -> 页面状态：loading、弹窗、tab、分页、错误
  -> 工具：useState / useReducer / 自定义 Hook
  -> 目标：降低耦合和无意义重渲染
```

### 推荐回答

我会按业务职责拆状态，不把所有状态塞进一个大对象。Reolink 移动端主要拆成三类：设备状态、消息状态、页面展示状态。

设备状态包括在线/离线、连接中、预览中、回放中等和设备生命周期相关的数据。消息状态包括告警消息、未读状态、推送入口状态。页面展示状态包括 loading、弹窗、筛选条件、当前 tab、分页等 UI 状态。

技术实现上，简单 UI 状态用 useState；复杂状态流转，比如设备连接、预览、回放这类状态机式逻辑，用 useReducer；跨组件复用逻辑抽成自定义 Hook，比如 useDeviceStatus、useMessageList。

这样拆的好处是状态边界清楚，一个状态变化不会带动整个页面无意义重渲染，也更方便写 Jest 或 RNTL 测试。

### 追问回答要点

- useState：简单局部 UI。
- useReducer：复杂状态流转。
- 自定义 Hook：复用业务逻辑。
- 性能：减少无关渲染，配合 useMemo/useCallback/React.memo。

### 追问逐题回答

#### 设备状态、消息状态、页面展示状态分别包含什么？

设备状态包括在线/离线、连接中、预览中、回放中、连接失败等；消息状态包括告警列表、未读数、已读状态、推送入口状态；页面展示状态包括 loading、弹窗、tab、筛选条件、分页、空状态和错误提示。

#### 为什么不用一个全局大状态对象？

全局大状态容易导致状态边界不清楚，一个字段变化触发大量无关组件更新，也会让测试和排查变困难。按业务职责拆分后，状态变化范围更小，组件依赖更清楚，也更容易定位问题。

#### useState、useReducer、自定义 Hook 怎么分工？

useState 用于简单 UI 状态，比如弹窗、loading、tab；useReducer 用于复杂状态流转，比如设备连接、预览、回放这类有多个状态和事件的场景；自定义 Hook 用于复用业务逻辑，比如 useDeviceStatus、useMessageList，把接口请求、状态流转和异常处理封装起来。

#### 状态拆分如何减少无意义渲染？

状态拆分后，组件只订阅自己真正需要的状态。比如消息列表更新不应该影响设备预览区域，弹窗状态变化也不应该让整个设备列表重渲染。再配合 React.memo、useMemo、useCallback，可以进一步减少列表项和子组件重复渲染。

#### 状态拆分和测试用例有什么关系？

状态拆得清楚后，测试可以按状态流转写得更稳定。比如设备连接从 loading 到 success/fail，可以用 Jest 测 reducer 或 hook；页面交互可以用 RNTL 测按钮点击后的状态变化；核心端到端链路再用 Detox 覆盖。

## Q13：长列表卡顿怎么定位？

### 速记链路

```text
先判断瓶颈
  -> 数据量 / 渲染次数 / 图片资源 / JS 线程
  -> 工具：RN Monitor、Flipper、Profiler
  -> 组件：FlatList / SectionList
  -> 参数：initialNumToRender、windowSize、maxToRenderPerBatch
  -> 优化：memo、useCallback、缩略图、缓存
```

### 推荐回答

我会先判断卡顿来源：是数据量太大、列表渲染太重，还是图片资源过大。

React Native 长列表一般优先检查 FlatList 或 SectionList，而不是直接用 ScrollView 渲染大量数据。重点看 keyExtractor 是否稳定，initialNumToRender、windowSize、maxToRenderPerBatch、removeClippedSubviews 等参数是否合理，以及 item 组件是否因为 props 或 inline 函数反复重渲染。

定位工具上，我会先用 RN 性能面板、React DevTools Profiler、Flipper 做初步判断；必要时结合 Android Studio Profiler 或 Xcode Instruments 看 JS 线程和 UI 线程是否阻塞。

优化上主要做列表虚拟化、item 组件 React.memo、useCallback 固定事件函数、useMemo 缓存计算结果、图片缩略图和缓存、避免在列表项里做复杂计算。

### 追问回答要点

- 不用 ScrollView 承载大量数据。
- JS 线程阻塞：复杂计算、频繁 setState。
- UI 线程掉帧：图片、布局、原生渲染压力。
- 重点列表场景真机验证是优化验证口径，不说成全量线上帧率指标。

### 追问逐题回答

#### 你怎么判断是数据量问题、渲染问题还是图片问题？

先看卡顿发生时机。如果数据一多就卡，优先看列表虚拟化和分页；如果每次状态变化都卡，优先看重复渲染和 item 组件；如果滚动到图片区域明显卡，优先看图片尺寸、缓存和加载策略。工具上用 RN 性能面板、React DevTools Profiler、Flipper 或原生 profiler 辅助判断。

#### FlatList 哪些参数会影响性能？

常见参数包括 initialNumToRender、windowSize、maxToRenderPerBatch、removeClippedSubviews、keyExtractor。它们会影响首屏渲染数量、窗口外组件保留数量、每批渲染数量和列表项复用稳定性。参数要结合页面复杂度和设备性能调，不是越大越好。

#### 为什么不直接用 ScrollView 渲染大量数据？

ScrollView 会一次性渲染所有子项，数据量大时会造成首屏慢、内存高和滚动卡顿。FlatList/SectionList 有虚拟化机制，只渲染可见区域附近的内容，更适合大量列表数据。

#### React.memo、useCallback、useMemo 分别解决什么？

React.memo 用于避免 props 没变时子组件重复渲染；useCallback 用于稳定函数引用，避免传给子组件的事件函数每次都变；useMemo 用于缓存计算结果，避免每次渲染都重新计算。但它们不是越多越好，只在渲染成本或引用稳定确实有收益时使用。

#### Flipper、RN Performance Monitor、Android Studio Profiler、Xcode Instruments 怎么配合使用？

RN Performance Monitor 可以快速看 JS/UI 帧率趋势；Flipper 适合看 RN 调试、网络、日志和部分性能信息；Android Studio Profiler / Xcode Instruments 更适合深入看原生线程、内存和 CPU。一般先用轻量工具定位方向，再用原生 profiler 深挖。

## Q19：介绍一下 GogoalShop 跨境商城项目。

### 速记链路

```text
项目背景：跨境电商商城
  -> 问题：首页资源体积大、首屏慢
  -> 动作：代码分割、依赖治理、Tree Shaking、图片字体优化
  -> 工程：npm -> pnpm、多语言工具
  -> 结果：JS 3MB -> 600KB，LCP 4s -> 2s 内
```

### 推荐回答

GogoalShop 是一个跨境电商商城项目，我在里面作为核心开发，主要负责商城性能优化、工程治理和业务工具建设。

当时比较典型的问题是首页资源体积大、加载链路长，影响用户访问体验和转化。我先通过 Lighthouse、Performance 和 bundle analyzer 分析首屏资源，定位关键 JS、图片、字体和非首屏模块的加载问题。

优化上主要做了代码分割、依赖替换、Tree Shaking、图片和字体加载优化。结果是关键 JavaScript 资源从 3MB 降到 600KB，首页 LCP 从 4s 降到 2s 以内，用户跳出率从 30% 降到 20%。

工程化方面，我推动包管理工具从 npm 迁移到 pnpm，规范依赖安装和构建流程，依赖安装时间从 10s 降到 5s 以内。

此外我还做了 Chrome 翻译数据抓取插件和 Node 自动写入服务，用来减少多语言配置的重复操作和人工出错。

### 追问回答要点

- 优先级：先性能瓶颈，再工程治理，再效率工具。
- 和 Reolink Web 区别：GogoalShop 更强调性能专项和工具建设。
- 数据口径：按优化前后 Lighthouse / 打包体积 / 业务统计对比讲。

### 追问逐题回答

#### 你作为核心开发具体负责什么？

我主要负责首页性能专项、部分工程化治理和多语言效率工具。性能专项包括资源分析、代码分割、依赖治理、图片和字体优化；工程化包括 npm 到 pnpm 迁移；工具建设包括 Chrome 翻译数据抓取插件和 Node 自动写入服务。

#### 首页性能问题是怎么暴露出来的？

主要通过用户访问体验、Lighthouse/Web Vitals 指标和本地 Performance 分析暴露。表现是首页资源体积大、首屏加载链路长、LCP 偏高。随后再用 bundle analyzer 和 Network 瀑布流定位具体资源来源。

#### 性能优化、工程化治理、效率工具三块的优先级怎么排？

优先处理直接影响用户体验和业务转化的性能问题；然后处理影响团队长期效率的工程化治理，比如 pnpm 和规范流程；最后处理重复人工成本高的效率工具，比如多语言配置。这个顺序是按业务影响和问题紧急度排的。

#### 这个项目和 Reolink Web 商城的技术侧重点有什么区别？

GogoalShop 侧重点是首页性能专项、资源体积治理、依赖治理和多语言工具；Reolink Web 更偏品牌官网商城，侧重点是商品内容、活动页交付、SEO、多端适配和 LCP。两个都是跨境商城，但技术目标不同。

## Q20：JS 体积从 3MB 到 600KB 具体怎么做？

### 速记链路

```text
先分析 bundle
  -> 找大依赖、重复依赖、首屏无关模块
  -> 代码分割：dynamic import / next/dynamic
  -> 依赖治理：按需引入、轻量替换、ESM
  -> Tree Shaking：sideEffects、构建产物验证
  -> 验证：bundle analyzer + Network
```

### 推荐回答

我会先用打包分析工具看体积来源，比如哪些依赖最大、是否存在重复依赖、哪些非首屏代码被打进首页 bundle。

具体优化分几类：

第一是代码分割。页面级和非首屏模块使用动态导入，比如 next/dynamic 或 import()，避免首页一次性加载不需要的模块。

第二是依赖治理。检查工具库、日期库、图表库这类大包，如果只使用少量能力，就换成轻量库或按需引入，避免整包导入。

第三是 Tree Shaking。优先选择支持 ESM 的依赖，检查 sideEffects 配置，避免 CommonJS 或错误构建配置导致无效代码无法摇掉。

第四是重复依赖排查。通过 bundle analyzer 和 pnpm why 观察是否存在多个版本依赖被重复打包。

最后通过构建产物体积、Network 下载体积和 Lighthouse 结果对比，验证优化确实减少了首屏负担。

### 追问回答要点

- 工具：`@next/bundle-analyzer`、构建产物分析、`pnpm why`。
- 手段：动态导入、依赖替换、按需引入、ESM、sideEffects。
- 风险：不能只把体积转移到后续关键路径。

### 追问逐题回答

#### 你用什么工具分析 bundle？

主要用 @next/bundle-analyzer 看构建产物里哪些模块体积大、是否有重复依赖；也会结合 Network 下载体积和 Lighthouse 看首屏资源影响。如果是 pnpm 项目，可以用 pnpm why 查某个包为什么被安装和引用。

#### 哪些依赖最大，怎么判断是否替换？

通常关注工具库、日期库、图表库、富文本或多媒体相关库。判断是否替换时看三个点：是否只用了少量能力、是否支持按需引入、是否有更轻量替代。替换前要确认 API 差异和业务风险，不能为了体积盲目替换。

#### next/dynamic / 动态 import 用在哪里？

用在非首屏组件、重型组件、低频弹窗、图表模块、复杂交互模块这些不需要首屏立即加载的地方。目标是减少首页初始 bundle，让用户先看到核心内容。

#### 重复依赖怎么排查？

通过 bundle analyzer 看是否打进多个版本的同类依赖，再用 pnpm why 或 lockfile 查依赖来源。处理方式可能是统一版本、升级依赖、替换包，或者调整依赖声明。

#### 优化后怎么验证不是只转移了体积？

除了看首页 bundle 下降，还要看用户关键路径是否变好，比如 LCP、Network 下载体积、交互延迟、懒加载模块加载时机。如果只是把首屏体积挪到用户马上点击的路径上，体验不一定变好，所以要结合业务路径验证。

## Q21：LCP 元素是什么？

### 速记链路

```text
LCP 不是固定元素
  -> Lighthouse / Performance 定位
  -> 图片型：Banner、主图、活动图
  -> 文本型：首屏标题、核心文案
  -> 图片优化：尺寸、格式、priority/preload
  -> 文本优化：字体、CSS、SSR/SSG 输出
```

### 推荐回答

LCP 元素不是固定的，要看具体页面。商城首页或活动页里，常见 LCP 元素一般是首屏 Banner、商品主图、活动主视觉图，或者首屏最大标题块。

我会用 Lighthouse 明细或 Chrome Performance 里的 LCP marker 先确认它是图片还是文本。

如果是图片型 LCP，优化重点是图片格式、尺寸、CDN、加载优先级、避免首屏图片被 lazy loading 误伤。Next.js 项目里可以结合 next/image、priority 或 preload 思路提升首屏图片加载优先级。

如果是文本型 LCP，重点看字体加载、CSS 阻塞和 SSR/SSG 输出时机，比如使用 next/font 减少字体阻塞和布局抖动。

所以 LCP 优化的第一步不是直接改代码，而是先确认最大内容元素和加载链路。

### 追问回答要点

- 图片型 LCP：格式、尺寸、CDN、priority/preload。
- 文本型 LCP：字体、CSS、SSR/SSG。
- 验证：优化前后比较 LCP 时间和 Network 瀑布流。

### 追问逐题回答

#### 首页 LCP 是图片还是文本？

这个不能固定说，要以具体页面测量结果为准。商城首页常见是首屏 Banner 或主视觉图，也可能是首屏最大标题块。面试时我会说先通过 Lighthouse 或 Performance 确认 LCP element，再针对图片或文本采取不同策略。

#### 图片型 LCP 怎么优化？

优化图片格式、尺寸、压缩质量和 CDN；首屏图不要 lazy loading；必要时用 priority 或 preload 提升加载优先级；移动端和桌面端使用合适尺寸的响应式图片，避免加载过大的桌面图。

#### 文本型 LCP 怎么优化？

重点优化字体加载、CSS 阻塞和服务端输出。可以用 next/font 或字体预加载减少字体阻塞，确保关键文本在 SSR/SSG 阶段输出，减少客户端等待接口后才渲染的情况。

#### next/image、next/font 分别解决什么问题？

next/image 主要解决图片尺寸、格式、懒加载、响应式图片和加载优先级问题；next/font 主要解决字体加载、字体文件托管和布局抖动问题。两者都服务于首屏体验和 Web Vitals。

#### LCP 从 4s 到 2s 以内的验证方式是什么？

通过优化前后 Lighthouse、Performance、Network 瀑布流和构建产物对比验证。口径上我会说这是重点页面在测试环境和上线前后对比中的结果，不说成所有用户环境的绝对值。

## Q25：介绍一下 Shopify 自研应用项目。

### 速记链路

```text
业务背景：替代第三方付费应用
  -> 两款应用：自定义印号、黑名单拦截
  -> 技术栈：React/Vite/TS + GraphQL + Prisma
  -> 部署：Docker 镜像化 + Shell 脚本
  -> 结果：应用上架、降低订阅成本、沉淀文档
```

### 推荐回答

Shopify 自研应用是为了解决团队降本和业务定制化需求。原来一些商品定制、黑名单下单拦截能力依赖第三方付费应用，持续订阅有成本，而且定制能力有限。

我独立完成了两款应用：自定义印号 TT-Options 和黑名单下单拦截 TTBlacklist - Order Guard。

技术上，前端使用 React、Vite、TypeScript 做 Shopify Admin 侧配置页面；接口层对接 Shopify GraphQL；数据层使用 Prisma 管理店铺安装信息、商品配置、黑名单规则和拦截记录；部署上用 Docker 解决 Node 版本、系统依赖和服务器环境不一致的问题。

最终两款应用完成开发与应用商店上架，替代 2 款第三方付费能力。年节省数万元这个数据按原第三方应用月费、店铺数量和订阅周期估算，不是收入指标。

### 追问回答要点

- 自研原因：降本 + 定制化。
- 两款应用：商品定制配置、黑名单订单拦截。
- 技术链路：React/Vite/TS + GraphQL + Prisma + Docker。
- 成本口径：订阅成本节省，不说成利润增长。

### 追问逐题回答

#### 为什么要自研而不是继续买第三方应用？

主要是降本和定制化。第三方应用有持续订阅成本，而且业务规则受限，比如商品定制和黑名单拦截需要更贴合团队流程。自研可以控制业务逻辑、数据结构和部署方式，长期成本更低。

#### 两款应用分别解决什么业务问题？

TT-Options 解决商品自定义印号或定制配置问题，让运营可以配置商品定制选项。TTBlacklist - Order Guard 解决黑名单下单拦截问题，对高风险用户或订单做规则判断和拦截/标记。

#### 你独立负责的边界是什么？

我独立负责应用前端配置页、GraphQL 接口对接、Prisma 数据模型、Docker 部署、Shell 发布脚本和应用上架流程。涉及业务规则确认、审核材料、线上运营流程，仍然需要和团队协作。

#### Shopify Admin 侧配置页怎么设计？

配置页重点是让运营能管理商品定制选项、黑名单规则和开关状态。前端用 React/Vite/TypeScript，表单提交后通过接口写入 Prisma 数据模型，再通过 Shopify GraphQL 或 Admin API 与店铺数据关联。

#### 替代 2 款第三方应用的成本节省怎么估算？

按第三方应用月费、店铺数量和 12 个月订阅周期估算。这个是订阅成本节省，不是 GMV 或利润增长。面试时我会明确这是成本估算口径。

## Q32：你搭建 ESLint + Prettier、Commitlint + Husky 的目的是什么？

### 速记链路

```text
问题：团队风格不一致、低级问题进仓库
  -> ESLint：代码质量规则
  -> Prettier：格式统一
  -> Commitlint：提交信息规范
  -> Husky：提交前自动拦截
  -> 目标：减少人工 review 的格式成本
```

### 推荐回答

这套规范的目的不是为了“形式统一”，而是减少团队协作里的低级问题，把代码风格和提交规范前置到本地开发阶段。

ESLint 主要解决代码质量和潜在问题，比如未使用变量、Hooks 规则、可能的错误写法。Prettier 主要解决格式统一，避免团队在缩进、换行、分号这些问题上浪费 review 时间。

Commitlint + Husky 主要是把提交信息和提交前检查自动化。Husky 可以在 pre-commit 或 commit-msg 阶段触发 lint、format 或 commit message 校验；Commitlint 保证提交信息符合约定，后续排查版本、生成 changelog 或回溯问题更方便。

我理解规范不能一刀切。历史项目接入时要先处理增量代码，必要时对旧代码分批修复，避免一次性改动过大影响业务迭代。

### 追问回答要点

- ESLint：质量问题。
- Prettier：格式统一。
- Husky：本地钩子。
- Commitlint：提交语义。
- 取舍：先控增量，再治理存量。

### 追问逐题回答

#### ESLint 和 Prettier 分别解决什么问题？

ESLint 解决代码质量和潜在错误，比如未使用变量、Hooks 规则、可能的问题写法；Prettier 解决格式统一，比如缩进、换行、分号、引号。ESLint 更偏质量，Prettier 更偏格式。

#### Commitlint 和 Husky 如何接入提交流程？

Husky 负责 Git hooks，比如 pre-commit 或 commit-msg；Commitlint 在 commit-msg 阶段校验提交信息是否符合规范。pre-commit 阶段也可以接 lint-staged，只检查本次改动文件，减少全量检查成本。

#### 规范太严格影响效率怎么办？

先区分规则价值。影响质量的规则保留，比如 Hooks、潜在错误；纯风格问题交给 Prettier 自动修复。历史项目接入时先控制增量，不一次性要求全项目修完，避免影响业务交付。

#### 如何处理历史代码不符合规范的问题？

先做增量治理，对新增和修改文件执行规范；存量代码分批处理，或者在业务迭代触达时顺手修复。不要一次性格式化大量历史文件，否则 diff 太大，review 成本和风险都高。

#### 代码规范如何和团队 code review 配合？

自动化工具处理格式和基础规则，code review 更关注业务逻辑、边界条件、性能、可维护性和安全问题。这样 review 不会浪费在缩进和命名这类可自动处理的问题上。

## Q35：React Hooks 的核心使用原则是什么？

### 速记链路

```text
规则：只在顶层调用 Hook
  -> 状态粒度：不要过大也不要过碎
  -> 副作用：useEffect 管外部同步和清理
  -> 复用：自定义 Hook 抽业务逻辑
  -> 性能：useMemo/useCallback 只在必要时用
```

### 推荐回答

我理解 React Hooks 的核心是让函数组件具备状态和副作用管理能力，但关键不是“会用 API”，而是把状态、副作用和复用逻辑拆清楚。

useState 适合简单局部状态；useReducer 适合复杂状态流转，比如设备连接、预览、回放这类状态机式逻辑；useEffect 处理副作用，但依赖项要基于实际使用的变量来写，避免漏依赖导致闭包问题，也避免不必要依赖导致重复请求。

useMemo 和 useCallback 不是越多越好，只有在计算成本较高、或者为了稳定传给子组件的引用时才使用。React.memo 也只有在 props 稳定、组件渲染成本较高时才有意义。

自定义 Hook 的边界是复用业务逻辑，而不是把页面代码简单搬进去。比如 useDeviceStatus、useMessageList 这类 Hook 可以封装接口请求、状态流转和异常处理，让页面组件更关注组合和展示。

### 追问回答要点

- useEffect：依赖项按使用变量确定。
- useMemo/useCallback：解决计算或引用稳定，不滥用。
- React.memo：props 稳定才有效。
- 自定义 Hook：复用逻辑，不承载页面所有职责。

### 追问逐题回答

#### useEffect 依赖项怎么判断？

依赖项应该包含 effect 内部使用到、且来自组件作用域的变量和函数。不能为了少执行就漏依赖，否则容易出现闭包旧值；如果依赖导致频繁执行，要考虑拆分 effect、稳定函数引用或调整状态结构，而不是强行忽略规则。

#### useMemo 和 useCallback 什么时候不该用？

计算很轻、组件不频繁渲染、没有传给 memo 子组件的引用，不需要用。滥用 useMemo/useCallback 会增加代码复杂度，也有维护成本。只有在计算成本高、引用稳定能减少子组件渲染时才有价值。

#### 自定义 Hook 的边界是什么？

自定义 Hook 应该封装可复用的状态和副作用逻辑，比如请求、状态流转、异常处理。它不应该变成页面组件垃圾桶，把所有页面逻辑都塞进去。页面负责组合和展示，Hook 负责可复用逻辑。

#### React.memo 为什么有时没效果？

常见原因是 props 每次都变，比如传入 inline 函数、对象、数组；或者组件本身渲染成本很低，memo 的比较成本反而不划算。React.memo 需要和稳定 props、useCallback、useMemo 配合才更有效。

#### Hooks 状态拆分如何影响可测试性？

状态拆分后，每个 Hook 或 reducer 的输入输出更清楚，可以单独测状态流转和异常分支。比如设备连接状态可以独立测试 loading、success、fail，而不用启动整个页面。

## Q36：TypeScript 在项目里解决了什么问题？

### 速记链路

```text
核心价值：提前暴露错误
  -> 接口类型：减少字段误用
  -> 组件 props：约束调用边界
  -> 业务状态：限制非法状态
  -> Native Module：约束入参和返回值
  -> 不是替代运行时校验
```

### 推荐回答

TypeScript 在项目里主要解决三个问题：接口契约、组件契约和重构安全。

接口契约上，后端或 GraphQL 返回的数据结构可以定义类型，减少字段写错、空值未处理的问题。组件契约上，props 类型能明确组件需要什么数据，比如商品卡片需要 title、price、image、href、tags，调用方传错字段时能提前发现。

在 React Native 里，TypeScript 也能约束 Native Modules 的 JS 调用层，比如入参、返回值、错误码，减少 JS 和原生协作时的低级错误。

在 Shopify 项目里，Prisma schema 生成的类型也能帮助服务端处理店铺信息、黑名单规则、配置数据时更稳定。

我不会为了类型而类型，业务里尽量避免 any 泛滥，但也会在第三方库类型不完整时做局部类型补充或封装，保证主要业务链路类型清晰。

### 追问回答要点

- 组件 props 类型：约束调用方。
- 接口类型：减少字段错误和空值问题。
- Native Modules：约束 JS/原生边界。
- Prisma：数据模型类型化。
- any：可临时兜底，但不能扩散到核心链路。

### 追问逐题回答

#### 组件 props 类型怎么设计？

先根据组件职责定义必要字段和可选字段。比如商品卡片需要 title、image、href，price、tags 可以根据业务可选。复杂配置要拆成子类型，避免一个巨大 props。事件回调也要明确参数类型。

#### 接口返回类型怎么维护？

如果是 GraphQL，可以基于查询结构维护类型；普通接口则定义 response 类型，并在数据转换层做兜底处理。核心是不要让接口原始结构直接散落到各个组件里，而是通过 service 或 adapter 收口。

#### any 过多会带来什么问题？

any 会绕过类型检查，让 TypeScript 失去价值。字段写错、空值未处理、调用参数错误都会延后到运行时暴露。可以局部使用 unknown 或临时 any，但核心业务链路要尽量补齐类型。

#### 泛型在业务组件中有哪些实际场景？

常见场景是列表组件、表单组件、接口响应结构、选择器组件。比如通用 Select 可以用泛型约束 option 的 value 类型，接口响应可以用 ApiResponse<T> 表达不同业务数据。

#### TypeScript 如何减少跨团队联调成本？

类型能把接口字段、组件入参、Native Module 调用约定提前暴露出来。联调时如果字段变化或入参不匹配，编译阶段就能发现一部分问题，减少运行时才发现的低级错误。

## Q40：AI skills 辅助研发流程具体怎么设计？

### 速记链路

```text
输入标准化
  -> 需求拆解、接口联调、异常排查、测试补充、文档沉淀
  -> 10+ 模板约束上下文
  -> AI 产出初稿
  -> 人工 review
  -> lint/test/build/业务验证
```

### 推荐回答

我设计 AI skills 辅助研发流程时，核心原则是让 AI 参与“结构化辅助”，而不是替代工程判断。

流程主要覆盖 5 个环节：需求拆解、接口联调、异常排查、测试补充、文档沉淀。

需求拆解阶段，模板会要求明确业务背景、入口页面、涉及接口、异常分支、验收标准。接口联调阶段，会整理请求参数、返回结构、错误码和兜底状态。异常排查阶段，会要求先提供复现路径、日志、影响范围，再让 AI 辅助列排查方向，避免空猜。测试补充阶段，会根据核心链路生成用例清单，再由人工筛选哪些适合 Jest、RNTL 或 Detox。文档沉淀阶段，会把最终结论整理成新人可复用的排查手册或任务模板。

这套流程沉淀了 10+ 结构化任务模板，覆盖 5 类高频研发场景。它的价值是提高需求理解、异常排查和文档输出的一致性，但最终仍然要靠人工 review、测试验证和业务验收。

### 追问回答要点

- AI 不替代判断，只辅助上下文组织和重复分析。
- 校验方式：diff review、lint、test、build、人工验收。
- 不适合 AI 的任务：业务决策、权限安全策略、无上下文的复杂重构。

### 追问逐题回答

#### 需求拆解模板包含哪些字段？

包含业务背景、目标用户、入口页面、核心流程、涉及接口、状态流转、异常分支、验收标准、影响范围和待确认问题。模板的价值是把需求从一句话拆成可开发、可验证的任务。

#### 代码草拟后如何进行人工校验？

先看 diff 是否符合需求，再检查是否符合现有目录结构、组件风格、类型约束和边界处理。然后跑 lint、test、build，必要时本地验证核心流程。AI 草稿只能作为初稿，不能绕过 review 和验证。

#### 异常排查如何避免 AI 胡乱猜？

先给足事实：复现步骤、日志、错误堆栈、影响范围、最近改动、相关代码。要求 AI 基于证据列假设和验证步骤，而不是直接给结论。没有日志或复现路径时，不让 AI 编原因。

#### 测试补充如何确保覆盖真实核心链路？

先从业务核心链路反推测试，而不是从代码覆盖率反推。比如设备接入、消息提醒、录像回放是高频链路，就覆盖成功、失败、空状态、异常提示和关键交互。低价值 UI 不用过度写 E2E。

#### 文档沉淀如何复用到新人 onboarding？

文档不只写结论，还要写背景、入口、接口、常见异常、排查步骤和验收方式。新人接手类似问题时，可以按文档找到上下文和处理路径，减少反复问人的成本。

## Q43：你简历中最有挑战的项目是哪一个？

### 速记链路

```text
选择：Reolink RN 移动端
  -> 难点：设备状态多、预览链路长、兼容要求高
  -> 动作：状态拆分、Native Modules 协作、异常兜底、测试
  -> 边界：RN 侧负责，不冒领底层播放器
  -> 收获：跨端复杂链路治理能力
```

### 推荐回答

我会选 Reolink 移动端项目。它的挑战不是单一页面复杂，而是业务链路长、状态多、兼容性和稳定性要求高。

比如设备接入、实时预览、消息提醒、录像回放这些链路，既涉及接口状态，也涉及设备状态、页面状态、权限状态和异常兜底。任何一个状态边界不清楚，都容易导致页面重复渲染、异常提示不一致或者回归问题。

我的处理方式是先拆状态和模块边界，把设备状态、消息状态、页面展示状态分开；然后针对 20+ 主流机型做适配，围绕长列表和核心页面渲染做性能治理；最后补充 20+ Jest、RNTL、Detox 测试用例，把设备接入、消息提醒、录像回放这些核心链路接入迭代验证。

如果重新做一次，我会更早把状态流转图、异常分支和测试用例清单前置到需求阶段，这样后续开发和回归成本会更低。

### 追问回答要点

- 难点：链路长、状态多、兼容性、稳定性。
- 个人动作：状态拆分、性能优化、测试补充。
- 团队结果：整体质量、业务规模、版本发布稳定性等不独占。

### 追问逐题回答

#### 最大难点是业务复杂、性能问题还是协作链路？

Reolink 移动端最大的难点是业务链路和状态复杂，同时叠加性能和兼容性要求。它不是单个页面难，而是设备状态、消息状态、页面状态、权限状态、异常兜底都交织在一起。

#### 你独立解决了什么问题？

我独立或主要负责的是部分核心链路功能迭代、状态拆分、长列表和页面渲染优化、测试用例补充，以及相关异常分支处理。整体质量指标和产品规模是团队结果。

#### 哪些结果依赖团队协作？

版本发布稳定性、整体用户体验、业务规模都依赖产品、后端、原生、测试和前端团队协作。我可以讲自己负责的工程动作，但不会把这些结果说成个人独立完成。

#### 如果重新做一次，你会怎么改？

我会更早把状态流转图、异常分支清单、接口契约和测试用例清单前置到需求阶段。这样开发时状态边界更清楚，后期测试和回归成本会更低。

## Q44：你如何处理需求不清晰或频繁变更？

### 速记链路

```text
先澄清目标
  -> 拆用户路径和验收标准
  -> 标记不确定点和依赖方
  -> 低成本原型 / 分阶段交付
  -> 变更记录同步
  -> 防止边做边猜
```

### 推荐回答

我一般先把需求从“描述”拆成可确认的边界：业务目标、入口页面、用户路径、接口依赖、异常分支、验收标准。

如果是活动页或商品页，我会先和运营确认活动时间、商品信息、优惠规则、跳转入口；和设计确认响应式规则和关键交互；和后端确认接口字段、错误码和兜底逻辑。确认后再进入开发，避免边做边猜。

需求变更时，我会判断变更影响范围：是文案和配置调整，还是组件结构、接口字段、业务流程变化。小变更直接处理；影响核心链路的变更要重新确认排期和风险。

AI skills 在这里可以辅助生成需求拆解模板和验收清单，但最终需求边界一定要和业务、设计、后端确认。

### 追问回答要点

- 拆解维度：目标、入口、路径、接口、异常、验收。
- 活动页减少返工：检查清单 + 联调问题手册。
- 变更评估：配置变更 vs 结构/流程变更。

### 追问逐题回答

#### 你如何拆需求和确认边界？

我会拆成业务目标、用户路径、入口页面、接口依赖、状态流转、异常分支、验收标准。拆完后和产品、设计、后端对齐，确认哪些是本期必须做，哪些可以后置。

#### 如何和设计、运营、后端对齐？

和设计确认响应式规则、交互状态和视觉边界；和运营确认活动时间、商品信息、文案、跳转和埋点；和后端确认接口字段、错误码、缓存和兜底。对齐结果最好沉淀到文档或检查清单里。

#### 活动页场景如何减少返工？

通过组件复用、活动页检查清单和常见联调问题手册减少返工。上线前检查内容、交互、接口、SEO、性能、兼容、埋点和回滚，避免等到最后才发现遗漏。

#### AI skills 是否参与需求拆解？

可以参与，但只做辅助。AI skills 可以根据模板帮我检查是否漏了入口、接口、异常分支、验收标准，但最终需求边界必须由业务、设计、后端和开发共同确认。

## Q45：你有哪些短板？

### 速记链路

```text
承认边界
  -> Android/iOS 原生底层不是主方向
  -> AI 模型训练不是主方向
  -> 全栈是协作和轻量交付，不是后端专家
  -> 补强方式：RN 新架构、JSBridge、Node 服务实践
```

### 推荐回答

我会比较直接地说，Android/iOS 原生底层不是我的主方向。我的移动端能力主要集中在 React Native、TypeScript、跨端页面、状态管理、多机型适配、Native Modules 协作和测试补充。如果岗位强要求 Java/Kotlin、Android Framework、原生架构设计，我不是最匹配的人选。

AI 方向也是类似，我做的是 AI skills / AI Agent 辅助研发流程，不是 TensorFlow、PyTorch 或移动端模型部署。

后端方面，我能做 Node.js 服务、GraphQL 对接、Prisma 数据模型、Docker 部署这类前端相关后端能力，但不是纯后端工程师定位。

我的优势是把 Web 前端、React Native 跨端、商城业务、性能优化和工程化质量结合起来，适合复杂业务前端、跨端协作和工程效率建设方向。

### 追问回答要点

- 不说精通 Android Framework。
- 不说 AI 模型训练。
- Node 是辅助能力，不定位纯后端。
- 用边界换可信度。

### 追问逐题回答

#### Android 原生是不是短板？

是。Android 原生底层不是我的主方向。我能做 React Native 和 Native Modules 调用层协作，但不说自己精通 Java/Kotlin、Android Framework 或大型原生架构设计。

#### AI 模型训练是不是短板？

是。我没有把自己定位为 AI 模型工程师，也不说自己熟悉 TensorFlow/PyTorch 模型训练。我的 AI 经验是研发流程辅助和 AI skills 落地。

#### 后端能力到什么程度？

我能做前端相关的 Node.js 服务、GraphQL 对接、Prisma 数据模型和 Docker 部署，能支撑 Shopify 应用这类全链路项目。但我不是纯后端，不会把自己包装成后端架构方向。

#### 如果岗位要求 Java/Kotlin Framework，你怎么评估匹配度？

如果这是核心要求，我匹配度不高；如果岗位需要 React Native、WebView/H5、跨端融合、前端工程化，我更匹配。我会在沟通早期明确这个边界。

## Q47：简历里有哪些数据需要谨慎解释？

### 速记链路

```text
数据不夸大
  -> LCP：重点页面验证口径
  -> 成本节省：第三方订阅估算
  -> 发布效率：步骤和耗时估算
  -> 测试用例：覆盖核心链路
  -> 团队结果和个人动作分开讲
```

### 推荐回答

我会主动把几个数据的边界讲清楚。

`LCP 2.5s 内` 是 Reolink 重点活动页在 Lighthouse / Web Vitals 验证中的口径，不是所有页面、所有网络环境下的绝对值。

`20+ 主流机型` 是兼容验证范围，不是覆盖所有设备型号。它主要说明我参与了多分辨率、不同系统版本和高频页面的适配验证。

`重点列表场景真机验证` 是性能优化验证口径，主要说明我通过 FlatList 参数、组件 memo、图片加载策略改善滑动卡顿，不说成全量线上帧率指标。

`部署成功率 100%` 限定在我参与的发布周期内，主要指 Docker 化后环境类部署失败被消除。

`发布效率 70%` 是按部署步骤和单次发布耗时粗略估算，不是业务收入指标。

### 追问回答要点

- 面试官追数据来源时，先讲统计边界。
- 不把团队指标包装成个人指标。
- 指标不能自证时，讲自己的动作和验证方法。

### 追问逐题回答

#### LCP 2.5s 内怎么解释？

这是重点活动页在 Lighthouse / Web Vitals 验证中的口径。我会说明测试页面、测试方式和优化动作，不说成所有页面、所有用户网络环境下都稳定 2.5s。

#### 20+ 主流机型怎么解释？

这是兼容验证范围，主要覆盖常见屏幕尺寸、不同 Android/iOS 版本、刘海屏/非刘海屏、大屏/小屏等场景。不能说成覆盖所有机型。

#### 重点列表场景真机验证怎么解释？

这是针对列表滑动和高频交互的真机验证口径，重点看优化前后的卡顿改善。它不是全量线上帧率指标，也不代表所有页面、所有机型都达到同一表现。

#### 部署成功率 100% 怎么解释？

限定在我参与的发布周期内，Docker 化后环境类部署失败被消除。它不是说所有历史发布永远不会失败，而是说明 Docker 解决了宿主机环境差异导致的失败。

#### 发布效率 70% 怎么解释？

这是按人工部署步骤和单次发布耗时粗略估算的提效。原来 5 步操作脚本化后变成 1 步，所以人工操作成本下降。它不是收入指标。

## Q48：哪些表达不要说满？

### 速记链路

```text
不要说满
  -> 不说精通 Android Framework
  -> 不说实现播放器内核
  -> 不说做 AI 模型训练
  -> 不说全栈专家
  -> 不把团队指标说成个人独占
```

### 推荐回答

我不会说自己精通 Android Framework，因为我的移动端主线是 React Native，不是原生底层。

我也不会说自己有 AI 模型训练经验，因为我做的是 AI 辅助研发流程，不是深度学习框架或模型部署。

项目成果上，我不会把团队结果全部说成个人独立完成。比如自然流量增长、整体交付质量、业务转化都属于团队结果，我可以讲自己负责的核心链路、优化动作和测试补充。

性能数据上，我不会说所有页面都稳定在某个指标，只讲简历里明确的重点页面或核心场景。

Web Vitals 上报也不会说成固定接入内部监控系统，除非项目确实有对应平台。

### 追问回答要点

- 原生能力不说满。
- AI 模型不说满。
- 团队结果不说成个人独占。
- 监控和指标不虚构。

### 追问逐题回答

#### 为什么不要说精通 Android Framework？

因为我的实际项目主线是 React Native，不是 Android Framework。强说精通会被 Java/Kotlin、多线程、渲染机制、Framework 源码追问，很容易暴露不匹配。

#### 为什么不要说有深度学习模型训练经验？

因为我的 AI 经验是 AI skills 辅助研发流程，不是模型训练或部署。强说模型经验会被 TensorFlow/PyTorch、模型压缩、端侧推理追问，风险很高。

#### 为什么不要把团队结果说成个人独立完成？

因为整体质量、自然流量、业务转化这些结果通常依赖产品、设计、后端、测试和前端团队协作。把团队结果说成个人独立完成不可信，也容易被追问组织流程和数据来源。

#### 为什么不要把 Web Vitals 上报说成固定内部监控系统？

除非项目确实接入并且你能说清平台、指标采集和数据口径，否则不要这么说。更稳的表达是：主要用 Lighthouse 和 Performance 做实验室分析，如果项目有 Web Vitals 上报，再结合线上采样趋势。

#### 为什么不要说所有页面、所有机型、所有版本都稳定？

因为这是绝对化表达，几乎无法自证。更稳的说法是“重点页面”“核心链路”“主要机型”“参与周期内”“上线前后对比”，把数据放在可解释边界内。

## Q49：面试收口时如何定位自己？

### 速记链路

```text
核心定位
  -> React/Next.js Web 前端
  -> React Native 跨端能力
  -> 商城业务和性能优化
  -> 工程质量：测试、规范、部署、AI skills
  -> 按岗位选择主线，不四条线平均讲
```

### 推荐回答

如果是 Web 前端岗位，我会把自己定位成 React/Next.js 方向，优势是商城页面、SEO、LCP 性能优化、组件化和工程化治理。

如果是跨端岗位，我会强调 React Native、TypeScript、双端适配、核心业务链路、Native Modules 协作、Jest/RNTL/Detox 测试和移动端性能优化。

如果是工程化岗位，我会强调规范治理、依赖治理、测试接入、发布提效、AI skills 辅助研发流程沉淀。

如果岗位偏 Android 原生，我会明确说明 Android 原生不是我的主方向，但我可以匹配 React Native、Web/H5、跨端融合和前端工程化相关部分。

一句话总结：我不是纯原生、纯后端或 AI 模型工程师，我的核心价值在复杂业务前端、跨端开发和工程质量落地。

### 追问回答要点

- Web 岗：React/Next.js、SEO、LCP、商城。
- 跨端岗：RN、双端、测试、性能。
- 工程化岗：规范、依赖、测试、发布、AI skills。
- Android 原生岗：诚实降级匹配范围。

### 追问逐题回答

#### Web 岗怎么收口？

我会强调 React/Next.js、TypeScript、商城页面、SEO、LCP、组件化和工程化治理。核心价值是能把页面交付、性能体验和工程质量一起推进。

#### 跨端岗怎么收口？

我会强调 React Native、TypeScript、双端适配、设备接入/消息提醒/录像回放核心链路、Native Modules 协作、Jest/RNTL/Detox 测试和移动端性能优化。

#### 工程化岗怎么收口？

我会强调规范治理、依赖治理、测试接入、发布提效、Docker 部署、AI skills 辅助研发流程。核心价值是降低重复成本和提高交付质量。

#### Android 原生岗怎么收口？

我会诚实说明 Android 原生不是主方向，但如果岗位需要 React Native、Web/H5、跨端融合、JSBridge 或前端工程化协作，我是匹配的。如果岗位核心是 Java/Kotlin Framework，我不会强行包装。

// 外网代理
export https_proxy=http://127.0.0.1:3083
export http_proxy=http://127.0.0.1:3083

// 添加 GitHub 远程仓库，定期同步
git remote add mirror https://github.com/appsmithorg/appsmith.git （添加远程仓库）（只要一次，下面重复）

git checkout -b mirror-v1.9.11   (新建一个mirror-v1.9.11并切到)
git pull origin ce  （拉取ce分支最新代码都本地，且合并，等同于fetch+merge）
git pull mirror v1.9.11  (拉取appsmith的v1.9.11分支)
（要解决冲突）（耗时）
（手动检查修改，注意一些删除的文件，我们的不删，提示的时候保留）
测试运行（后端重新编译，前端安装依赖，耗时）
git commit -m 'xxx' （提交到本地仓库）
git checkout ce （切换到ce）
git pull origin ce  （合并前先拉取一下远程）
git merge mirror-v1.9.11
测试运行 （后端重新编译，前端安装依赖，耗时）
git commit -m 'xxx' (提交到本地仓库）
git push origin ce （将ce推到它的远程仓库）
注意一些文件，尽量不要该改，package.json,readme等等
# 通用执行框架

## 模块定位

这一层描述 `CommonFrameworkV1` 如何组织一次完整的代码合成任务，包括状态初始化、Prompt 构造、节点生命周期、主循环执行和后置验证。

---

## 二级子模块

### 1. CommonFrameworkV1总控引擎

目录：`02-通用执行框架/01-CommonFrameworkV1总控引擎/`

说明：该子模块文档用于拆解 `CommonFrameworkV1总控引擎` 的执行过程、关键职责和上下游关系。

### 2. Prompt与知识注入

目录：`02-通用执行框架/02-Prompt与知识注入/`

说明：该子模块文档用于拆解 `Prompt与知识注入` 的执行过程、关键职责和上下游关系。

### 3. 节点生命周期体系

目录：`02-通用执行框架/03-节点生命周期体系/`

说明：该子模块文档用于拆解 `节点生命周期体系` 的执行过程、关键职责和上下游关系。

### 4. CommonFrameworkV1节点逐个拆解

目录：`02-通用执行框架/04-CommonFrameworkV1节点逐个拆解/`

说明：该子模块文档用于拆解 `CommonFrameworkV1节点逐个拆解` 的执行过程、关键职责和上下游关系。

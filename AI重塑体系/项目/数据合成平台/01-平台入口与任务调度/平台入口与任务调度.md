# 平台入口与任务调度

## 模块定位

这一层负责把外部请求接入平台、映射到具体 pipeline，并把执行状态缓存和暴露出来。它对应整个系统从 HTTP 入口到任务调度的第一段链路。

---

## 二级子模块

### 1. 入口层与请求分发

目录：`01-平台入口与任务调度/01-入口层与请求分发/`

说明：该子模块文档用于拆解 `入口层与请求分发` 的执行过程、关键职责和上下游关系。

### 2. ProxyService与任务调度

目录：`01-平台入口与任务调度/02-ProxyService与任务调度/`

说明：该子模块文档用于拆解 `ProxyService与任务调度` 的执行过程、关键职责和上下游关系。
